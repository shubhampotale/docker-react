language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t shubhamdockerpractice/docker-react	-f Dockerfile.dev .

script:
  - docker run -e CI=true shubhamdockerpractice/docker-react npm run test
